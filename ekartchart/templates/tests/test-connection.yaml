apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ekartchart.fullname" . }}-test-connection"
  labels:
    {{- include "ekartchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ekartchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
